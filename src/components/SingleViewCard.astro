---
const { data } = Astro.props;
const { data } = Astro.props;
---

<article>
  <div>
    <h2>{data.name}</h2>
    <img src="/dusomerihimlene.avif" alt="" />
  </div>
  <div>
  <div>
    <h2>{data.name}</h2>
    <img src="/dusomerihimlene.avif" alt="" />
  </div>
  <div>
    <h3>Beskrivelse af værket:</h3>
    <p>{data.description}</p>
  </div>
  <ul>
    <li><p>Lavet af: <span>{data.artistname}</span></p></li>
    <li><p>Lokation: <span>{data.location}</span></p></li>
    <li><button>Tilføj til rute</button> <img src="/addtorouteicon.avif" alt="icon" /></li>
    <li><button>Links</button> <img src="/linkdropdown.avif" alt="icon" /></li>
  </ul>
    <p>{data.description}</p>
  </div>
  <ul>
    <li><p>Lavet af: <span>{data.artistname}</span></p></li>
    <li><p>Lokation: <span>{data.location}</span></p></li>
    <li><button>Tilføj til rute</button> <img src="/addtorouteicon.avif" alt="icon" /></li>
    <li><button>Links</button> <img src="/linkdropdown.avif" alt="icon" /></li>
  </ul>
</article>

<style>
  article {
  article {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 50px;
    max-width: 700px;
    gap: 50px;
    max-width: 700px;
    margin: auto;
  }
  }

  article p {
  article p {
    font-size: 0.8rem;
  }
  }

  article h3,
  h2 {
    margin-bottom: 1rem;
  }
  }

  article h2,
  li {
  article h2,
  li {
    border: 1px solid;
    border-image-slice: 1;
    border-image-source: var(--cta-button-background-color);
    padding: 0.8rem;
  }
    border-image-slice: 1;
    border-image-source: var(--cta-button-background-color);
    padding: 0.8rem;
  }

  li {
  li {
    border-top: 0;
    border-left: 0;
    border-right: 0;
  }
  }

  li:nth-child(3) {
  li:nth-child(3) {
    display: flex;
    justify-content: space-between;
    background-image: var(--cta-button-background-color);
    padding: 0.5rem;
  }
    padding: 0.5rem;
  }

  li:nth-child(4) {
  li:nth-child(4) {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  }

  li img {
  li img {
    width: 5%;
    height: 5%;
  }
  }

  ul p,
  button {
    font-size: 1.2rem;
  }

  button {
    display: flex;
    color: white;
    background-color: transparent;
  }

  article > div:first-child {
  article > div:first-child {
    grid-column: 1/3;
  }
  }
</style>

